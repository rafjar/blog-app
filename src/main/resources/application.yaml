server:
  port:
    8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:8081/blog-app
    username: rafal
    password: rafal
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
